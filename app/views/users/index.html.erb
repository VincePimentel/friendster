<%= render partial: "row",
  locals: {
    heading: "Friend Requests (Received)",
    users: @requests,
    type: "accept"
  } %>

<%= render partial: "row",
  locals: {
    heading: "Pending Requests (Sent)",
    users: @pending,
    type: "cancel"
  } %>

<%= render partial: "row",
  locals: {
    heading: "Find Friends",
    users: @users,
    type: "add"
  } %>
