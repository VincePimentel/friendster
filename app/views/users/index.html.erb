<%= render partial: "users_row",
  locals: {
    heading: "Friend Requests (Received)",
    users: @requests,
    type: "accept"
  } %>

<%= render partial: "users_row",
  locals: {
    heading: "Pending Requests (Sent)",
    users: @pending,
    type: "cancel"
  } %>

<%= render partial: "users_row",
  locals: {
    heading: "Find Friends",
    users: @users,
    type: "add"
  } %>
