<%= render partial: "row",
  locals: {
    heading: "Friend Requests (Received)",
    users: @requests,
    action: "accept"
  } %>

<%= render partial: "row",
  locals: {
    heading: "Pending Requests (Sent)",
    users: @pending,
    action: "cancel"
  } %>

<%= render partial: "row",
  locals: {
    heading: "Find Friends",
    users: @users,
    action: "add friend"
  } %>
