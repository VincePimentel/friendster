<%= form_for [post, @comment] do |f| %>
  <div class="form-row">
    <div class="col-9">
      <%= f.text_field :content, placeholder: "Leave a comment", class: "form-control form-control-sm" %>
    </div>

    <div class="col-3">
      <%= f.submit :post, class: "btn btn-primary btn-block btn-sm" %>
    </div>
  </div> <!-- form-row -->
<% end %>
