<nav class="navbar navbar-expand-md navbar-dark sticky-top bg-dark">
  <div class="container">

    <a class="navbar-brand" href="<%= root_path %>">Friendster</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsMain">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsMain">
      <ul class="navbar-nav ml-auto">

        <% if logged_in? %>
          <li class="nav-item">
            <%= request_notification %>
          </li>
          <li class="nav-item"> <!-- active -->
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to current_user.first_name, user_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navDropdown" data-toggle="dropdown"><%= icon("fas", "user-cog") %></a>
            <div class="dropdown-menu">
              <%#= link_to "Settings", "#", class: "dropdown-item" %>
              <%= link_to "Log Out", logout_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item active">
            <%= link_to "Log In", login_path, class: "nav-link" %>
          </li>
          <li class="nav-item active">
            <%= link_to "Register", signup_path, class: "nav-link" %>
          </li>
        <% end %>

      </ul>
    </div>

  </div>
</nav>
